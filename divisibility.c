//determine whether a number is odd or even
#include<stdio.h>
int main ()
{
  int x;
  printf("\n enter value of x:");
  scanf("%d",&x);
  if (x%2==0){
    printf("\n x is even");
  }
else(printf("x is odd"));
return 0;
}
